import {observable} from 'mobx' //, computed, autorun, action

/**todo: add method to sort ascending, should be saved as such */
class View {
  @observable shortComb =0
  @observable longComb =0
  
}

export default View